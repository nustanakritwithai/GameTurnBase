/**
 * Ring 0 locked combat baselines (Blueprint v3 §3.6.12).
 * Playtest tuning may adjust later; these are the documented starting points.
 */
export const BASELINE_HITSTUN_MS = 200
export const MOB_TELEGRAPH_MS = 280
export const GET_UP_INVULNERABLE_MS = 200

/** Get-up animation placeholder — duration not Ring 0 locked; framework only. */
export const GET_UP_ANIMATION_MS = 360
